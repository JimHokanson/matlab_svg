%{

%NOTES
%===================================

Currently the idea is to parse the Matlab figure into objects. This is
initiated with the command SVG.figure(figure_handle). After possible
manipulations the function create_svg is called on the output object.


Currently working on conversion_options class and size aspects


%}